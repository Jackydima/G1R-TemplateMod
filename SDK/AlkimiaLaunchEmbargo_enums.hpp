enum class EAlkimiaLaunchEmbargoState {
    Unknown = 0,
    Unavailable = 1,
    Effective = 2,
    Lifted = 3,
    UserExempt = 4,
    BuildExempt = 5,
    Disabled = 6,
    EAlkimiaLaunchEmbargoState_MAX = 7,
};

enum class EAlkimiaLaunchEmbargo_FileSource {
    TitleStorage = 0,
    HttpServer = 1,
    EAlkimiaLaunchEmbargo_MAX = 2,
};

